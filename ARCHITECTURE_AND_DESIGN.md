# 📘 Mini-Coroutine-Scheduler 协程调度器：零基础工程架构与事件循环全景指南

> **所属专业课**：《操作系统》《并发系统设计》《异步 I/O 与事件驱动架构》  
> **核心目标**：彻底理解现代操作系统中**进程/线程生命周期状态机、就绪队列轮转调度、最小堆定时器、用户态上下文切换（Context Switch）与事件循环（Event Loop）**的底层本质。掌握如何用几百行代码手工实现一个迷你操作系统的任务调度内核。

---

## 目录
1. [第一部分：工程规范、工具链与环境大解密](#第一部分工程规范工具链与环境大解密)
   * [1. 目录结构与极简纯粹设计哲学](#1-目录结构与极简纯粹设计哲学)
   * [2. Python 现代包规范：pyproject.toml 究竟在管什么？](#2-python-现代包规范pyprojecttoml-究竟在管什么)
   * [3. 零外部依赖哲学：为什么标准库就是最好的操作系统实验室？](#3-零外部依赖哲学为什么标准库就是最好的操作系统实验室)
2. [第二部分：核心原理、技术选型与调度内核全景](#第二部分核心原理技术选型与调度内核全景)
   * [1. 现实痛点与业务价值：为什么需要协程与事件驱动？](#1-现实痛点与业务价值为什么需要协程与事件驱动)
   * [2. 核心调度架构选型矩阵（Why this, not that?）](#2-核心调度架构选型矩阵why-this-not-that)
   * [3. 扫清“黑魔法”：生成器与用户态上下文切换推演](#3-扫清黑魔法生成器与用户态上下文切换推演)
   * [4. 全景 ASCII 任务状态流转与事件循环调度图解](#4-全景-ascii-任务状态流转与事件循环调度图解)
   * [5. 零基础手写复现通关路线卡（五步通关）](#5-零基础手写复现通关路线卡五步通关)

---

## 第一部分：工程规范、工具链与环境大解密

### 1. 目录结构与极简纯粹设计哲学

查看项目工程目录：

```text
mini-coroutine-scheduler/
├── ARCHITECTURE_AND_DESIGN.md  # 🌟 零基础架构设计与调度内核指南
├── scheduler.py                # 🔨 调度器内核 (Task/TCB, Scheduler, AsyncQueue, AsyncEvent)
├── test_scheduler.py           # 🧪 自动化测试套件 (验证状态机/定时器/异步队列/事件广播)
├── examples/
│   └── producer_consumer.py    # 💡 经典多协程协作实战演示范例
├── pyproject.toml              # ⚙️ 现代标准项目元数据配置文件
├── .gitignore                  # Git 忽略配置
└── README.md                   # 项目导航说明手册
```

#### 为什么用 Python 手写操作系统调度内核，而不是直接写 Linux 内核代码？
* **真实内核代码极其庞大复杂**：Linux 内核真正的完全公平调度器（CFS，Completely Fair Scheduler）有几万行 C 代码，充斥着红黑树旋转、特定 CPU 汇编与内存屏障，初学者直接看会被严重劝退。
* **高维解构本质**：通过 Python 的生成器（Generator）与标准库数据结构，我们能用**最直观、无噪点的 300 行代码**，把操作系统的**就绪队列、时间片轮转、小顶堆定时器、阻塞挂起机制**彻底具象化展现出来。

---

### 2. Python 现代包规范：`pyproject.toml` 究竟在管什么？

在过去，Python 项目充斥着 `setup.py`、`setup.cfg`、`requirements.txt`、`Pipfile` 等混乱配置。
现代 Python 官方推出了 **PEP 517 / PEP 621 标准**，规定统一使用 `pyproject.toml`：

```toml
[project]
name = "mini-coroutine-scheduler"
version = "0.1.0"
description = "A toy user-space Coroutine & Cooperative Task Scheduler..."
requires-python = ">=3.9"
dependencies = [] # 零外部依赖！
```

* **`requires-python = ">=3.9"`**：指定 Python 解释器最低版本（因为用到了类型注解如 `deque[Task]`）。
* **`dependencies = []`**：声明本项目不依赖任何 pip 第三方库，保证任何电脑拉下来即可秒级运行。

---

### 3. 零外部依赖哲学：为什么标准库就是最好的操作系统实验室？

本项目精选了 Python 内部最贴合底层计算机科学的 4 大内置模块：
1. **`collections.deque`**：**双端队列**。内部由双向链表+定长缓冲区构成，两端入队出队是严格的 $O(1)$，天然契合操作系统的**FIFO 就绪队列（Ready Queue）**。
2. **`heapq`**：**最小二叉堆（Min-Heap）**。操作系统定时器的黄金搭档，堆顶永远是最早到期的任务。
3. **`enum.Enum`**：严格类型安全的状态枚举，定义操作系统的经典进程五态。
4. **`time`**：提供高精度时间戳 `time.time()`，作为时钟发生器。

---

## 第二部分：核心原理、技术选型与调度内核全景

### 1. 现实痛点与业务价值：为什么需要协程与事件驱动？

在经典的“多线程/多进程”服务器中，每个请求都需要一个独立的操作系统线程：
* **痛点 1：内存过大**：每个 OS 线程需要 8MB 栈内存，1 万个并发连接直接吃掉 80GB 内存。
* **痛点 2：内核态切换昂贵**：操作系统抢占调度需要陷入内核，消耗上千个 CPU 时钟周期。
* **痛点 3：锁竞争与竞态**：多线程访问共享变量必须加互斥锁，容易引发死锁与性能暴跌。

**协程（Coroutine）的革命**：
* **轻量到极致**：协程运行在**用户空间（User Space）**，一个任务只占用几百字节，单机可以轻松并发 100 万个任务；
* **单线程无锁无并发安全问题**：协程是在单线程内交替执行的，不需要昂贵的互斥锁；
* **主动交出控制权**：遇到 I/O 或等待时主动 `yield` 休息，让给其他任务执行，CPU 利用率接近 100%！

---

### 2. 核心调度架构选型矩阵（Why this, not that?）

#### 决策一：调度机制选“协作式（Cooperative）”还是“抢占式（Preemptive）”？

| 机制 | 工作原理 | 优缺点 | 为什么本项目选它？ |
| :--- | :--- | :--- | :--- |
| **抢占式调度 (Preemptive)** | 操作系统通过硬件时钟中断，强行剥夺当前任务的 CPU 占用权。 |  能防止恶意死循环霸占 CPU；❌ 但需要内核级上下文切换，且代码任何一行都可能被中断，需要全局加锁保护。 | 操作系统内核使用 |
| **协作式调度 (Cooperative - 本项目)** | 任务自愿执行到某个阶段，通过 `yield` 主动说：“我做完了/我需要等数据，CPU 给别人用吧”。 |  **无锁并发、上下文切换成本极低（函数调用级别）**；❌ 若某个任务写了死循环不交出控制权，整个线程会卡住。 | **现代高并发框架（Node.js/Go）核心基石** |

#### 决策二：定时器数据结构为什么必须用最小二叉堆（Min-Heap）？

* ❌ **方案 A：普通数组 / 列表**：
  * 如果有 10000 个任务在 `sleep()`，调度器每隔一毫秒都要把这 10000 个任务从头遍历一遍，时间复杂度是 $O(N)$，CPU 瞬间被定时器遍历吃满！
*  **方案 B：最小堆（Min-Heap，本项目采用）**：
  * 最小堆把时间戳最小（也就是**最早该醒来的任务**）自动浮动在堆顶 `heap[0]`。
  * **复杂度**：调度器每轮只需要看一眼堆顶时间到了没有，检查开销是极速的 **$O(1)$**！弹出是 **$O(\log N)$**！

---

### 3. 扫清“黑魔法”：生成器与用户态上下文切换推演

很多同学认为 Python 的 `yield` 只是用来遍历列表的，**其实 `yield` 本质上是一个“用户态断点执行器”！**

#### 1. 任务控制块（TCB / Task Control Block）
在 Linux 内核里叫 `struct task_struct`，在本项目里叫 `class Task`：
```python
class Task:
    def __init__(self, coro, name=None):
        self.tid = Task._id_counter       # 任务唯一标识 (Thread ID)
        self.coro = coro                  # 协程生成器对象 (保存了内部局部变量与指令指针 PC)
        self.state = TaskState.READY      # 任务状态机
        self.wake_time = None             # 睡眠到期时间戳
```

#### 2. 上下文切换（Context Switch）的发生时刻
* **切入任务（Resume）**：调用 `self.coro.send(value)`，CPU 控制权跳转进协程内部，从上次暂停的行继续往下跑；
* **让出任务（Yield）**：协程执行到 `yield Syscall.Sleep(1.0)`，Python 自动将协程当下的全部局部变量保存在生成器对象内部，将执行控制权**交还给调度器主循环**！
* 这一切**完全不发生任何系统调用、不切换内核特权级**，速度比操作系统线程切换快 100 倍！

---

### 4. 全景 ASCII 任务状态流转与事件循环调度图解

#### 1. 操作系统经典的“任务五态流转模型”
```text
           spawn() 创建任务
                 │
                 ▼
         ┌──────────────┐ (调度器从队头弹出)
   ┌────►│  READY 就绪   ├────────────────┐
   │     └──────────────┘                │
   │            ▲                        ▼
   │            │ 唤醒(Wakeup)    ┌──────────────┐
   │ 队列有数据  └────────────────┤RUNNING 运行中│
   │                              └──────┬───────┘
   │                                     │
   │             yield Sleep(2.0)        │ yield Queue.get() (队列空)
   │                    ┌────────────────┴───────────────┐
   │                    ▼                                ▼
   │             ┌──────────────┐                 ┌──────────────┐
   │             │SLEEPING 睡眠 ├┐                │WAITING 阻塞  ├┐
   │             └┬─────────────┘│                └┬─────────────┘│
   │              └──────────────┘ (小顶堆维护)    └──────────────┘ (条件未满足)
   │                    │                                │
   └────────────────────┴────────────────────────────────┘
                                  │
                                  ▼ 执行完毕 (StopIteration)
                           ┌──────────────┐
                           │TERMINATED 终止│
                           └──────────────┘
```

#### 2. 调度器主循环（Event Loop）每轮运行节奏
```text
                  ┌─────────────────────────────────────┐
                  │       开始新一轮事件循环周期          │
                  └──────────────────┬──────────────────┘
                                     │
                                     ▼
        【步骤 1：时钟检查】检查 sleeping_heap 堆顶任务到期没？
         - 若当前时间 >= 堆顶时间：弹出任务，改状态为 READY，塞入就绪队列！
                                     │
                                     ▼
        【步骤 2：就绪出队】从 ready_queue 左侧 popleft() 取出下一个任务
         - 若就绪队列为空：休眠微秒级时间，防止 CPU 100% 盲转
                                     │
                                     ▼
        【步骤 3：上下文切入】调用 task.resume() 激活协程
         - 协程执行一段逻辑，遇到 yield 主动交出控制权
                                     │
                                     ▼
        【步骤 4：系统调用分发】根据协程 yield 出来的指令类型处理：
         - 若任务主动 Sleep：压入 sleeping_heap 最小堆
         - 若任务等待事件/队列：放入 waiting_tasks 集合
         - 若任务仅让出时间片：放回 ready_queue 队尾排队
         - 若任务执行完毕：销毁任务，标记为 TERMINATED
```

---

### 5. 零基础手写复现通关路线卡（五步通关）

当你准备自己亲手实现这个用户态协程调度器时，按照以下 5 个关卡递进：

```text
关卡 1：构建任务状态机与 TCB 任务控制块 (Task 类)
  ├── 目标：用 Enum 定义 READY, RUNNING, SLEEPING, WAITING, TERMINATED。
  └── 核心：Task 类封装生成器对象 coro，提供 resume() 方法。

关卡 2：就绪队列轮转与多任务交替执行 (Scheduler 核心)
  ├── 目标：用 deque 实现 ready_queue，提供 spawn() 创建并排队任务。
  ├── 核心：编写 run_until_idle() 循环，依次从就绪队列取出任务调用 resume()。
  └── 验证：实现两个死循环计数器协程，观察它们在控制台上交替打印 1, 2, 3...。

关卡 3：引入最小堆定时器与非阻塞 Sleep
  ├── 目标：用 heapq 实现 sleeping_heap。
  ├── 核心：遇到 yield ("SLEEP", seconds) 时，计算 wake_time 并压入堆。
  └── 验证：跑通 test_scheduler.py 测试 2，验证 0.1 秒的延时唤醒顺序无误。

关卡 4：实现异步同步原语 AsyncQueue (阻塞队列)
  ├── 目标：实现基于协程协作的生产者-消费者队列。
  ├── 核心：队列满时把生产者放入 waiting 集合；队列空时把消费者挂起；入队/出队时自动唤醒对方。
  └── 验证：跑通 test_scheduler.py 测试 3。

关卡 5：实现 AsyncEvent 条件广播与全量自动化测试
  ├── 目标：实现类似操作系统条件变量的事件通知机制。
  └── 验证：运行 python3 test_scheduler.py，4 项自动化测试全绿 PASS！
```
